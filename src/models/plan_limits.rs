/*
 * SimpleBilly API
 *
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PlanLimits : Per-plan numeric limits. `-1` in any field means unlimited.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PlanLimits {
    #[serde(rename = "maxConnectors")]
    pub max_connectors: i32,
    #[serde(rename = "maxInvoicesPerMonth")]
    pub max_invoices_per_month: i64,
    #[serde(rename = "maxUsers")]
    pub max_users: i32,
    #[serde(rename = "metered", skip_serializing_if = "Option::is_none")]
    pub metered: Option<std::collections::HashMap<String, i64>>,
    /// Connectors that are *not* included in this plan (require a higher tier). Empty = all connectors included on this plan.
    #[serde(rename = "paidConnectors")]
    pub paid_connectors: Vec<String>,
}

impl PlanLimits {
    /// Per-plan numeric limits. `-1` in any field means unlimited.
    pub fn new(max_connectors: i32, max_invoices_per_month: i64, max_users: i32, paid_connectors: Vec<String>) -> PlanLimits {
        PlanLimits {
            max_connectors,
            max_invoices_per_month,
            max_users,
            metered: None,
            paid_connectors,
        }
    }
}

