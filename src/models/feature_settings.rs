/*
 * SimpleBilly API
 *
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// FeatureSettings : Feature toggles for a tenant.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct FeatureSettings {
    /// Online shop / storefront module (default: enabled).
    #[serde(rename = "onlineshop")]
    pub onlineshop: bool,
    /// Bilanz (balance sheet) report.
    #[serde(rename = "reportBilanz")]
    pub report_bilanz: bool,
    /// BWA (betriebswirtschaftliche Auswertung).
    #[serde(rename = "reportBwa")]
    pub report_bwa: bool,
    /// EÜR (Einnahmen-Überschuss-Rechnung).
    #[serde(rename = "reportEuer")]
    pub report_euer: bool,
    /// Gewerbesteuer report.
    #[serde(rename = "reportGewerbesteuer")]
    pub report_gewerbesteuer: bool,
    /// GuV (profit & loss) report.
    #[serde(rename = "reportGuv")]
    pub report_guv: bool,
    /// KSt (Körperschaftsteuer) report.
    #[serde(rename = "reportKst")]
    pub report_kst: bool,
    /// UStVA (Umsatzsteuervoranmeldung).
    #[serde(rename = "reportUstva")]
    pub report_ustva: bool,
}

impl FeatureSettings {
    /// Feature toggles for a tenant.
    pub fn new(onlineshop: bool, report_bilanz: bool, report_bwa: bool, report_euer: bool, report_gewerbesteuer: bool, report_guv: bool, report_kst: bool, report_ustva: bool) -> FeatureSettings {
        FeatureSettings {
            onlineshop,
            report_bilanz,
            report_bwa,
            report_euer,
            report_gewerbesteuer,
            report_guv,
            report_kst,
            report_ustva,
        }
    }
}

