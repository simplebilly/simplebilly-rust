/*
 * SimpleBilly API
 *
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OffenlegungReport {
    /// Fristende (Abschlussstichtag + Frist).
    #[serde(rename = "deadline")]
    pub deadline: chrono::NaiveDate,
    /// Offenlegungsfrist in Monaten (§ 325 Abs. 4 HGB).
    #[serde(rename = "deadline_months")]
    pub deadline_months: i32,
    #[serde(rename = "items")]
    pub items: Vec<models::OffenlegungItem>,
    /// Annahme über die Kapitalmarktorientierung.
    #[serde(rename = "kapitalmarktorientiert")]
    pub kapitalmarktorientiert: bool,
    #[serde(rename = "note")]
    pub note: String,
    /// Berichtsjahr (laufendes Kalenderjahr).
    #[serde(rename = "year")]
    pub year: i32,
}

impl OffenlegungReport {
    pub fn new(deadline: chrono::NaiveDate, deadline_months: i32, items: Vec<models::OffenlegungItem>, kapitalmarktorientiert: bool, note: String, year: i32) -> OffenlegungReport {
        OffenlegungReport {
            deadline,
            deadline_months,
            items,
            kapitalmarktorientiert,
            note,
            year,
        }
    }
}

